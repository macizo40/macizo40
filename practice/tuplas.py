#testing the current values that a tupla does have 

tupla = (234,"text",[2,2,3,4,5,6],-50)

print("Values of the tupla are:{}".format(tupla))

#all values are inmutable so there is no way to change it

