#now since the modolues file is in the same path, you can just import it

#this is the way that is we have folders we can import the modules as a package
import messages.modules as modules
#now the code will run after the method is called
modules.hello()

